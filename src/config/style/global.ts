import { css } from '@emotion/react';

const global = css`
  * {
    box-sizing: border-box;
  }

  #root {
    width: 100%;
    height: 100vh;
  }
  button {
    border: none;
  }
`;

export default global;
